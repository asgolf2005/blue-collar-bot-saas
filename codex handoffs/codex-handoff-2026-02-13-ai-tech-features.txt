Codex Session Handoff
Date: 2026-02-15
Project: C:\Users\asgol\blue-collar-bot-saas

AI FEATURES FOR TECHNICIAN MOBILE PAGES
======================================

TASK OVERVIEW
-------------
Implement AI-powered features across all technician mobile pages (/tech/*) to create 
a differentiated "AI-native" field service experience. These features should make techs
faster, more accurate, and more profitable while working in the field.

CURRENT STATE
-------------
- Tech app exists with: today's jobs, job details, schedule, stats, photo upload
- Uses Lucide + Phosphor icons (see components/ui/icons.tsx)
- Dark/light mode glassmorphic design
- Supabase backend with real-time updates
- No AI features currently implemented

REQUIRED AI FEATURES (IMPLEMENT ALL)
====================================

PRIORITY 0 (START HERE - Build First)
-------------------------------------

### 1. VOICE-TO-JOB NOTES ⭐ HIGHEST PRIORITY

WHAT IT DOES:
Tech taps microphone → Speaks naturally → AI transcribes with Whisper → 
GPT formats into professional job notes → Saves to job

USER FLOW:
1. Tech on job detail page taps "Voice Note" button
2. Holds to record (like WhatsApp voice message)
3. Releases, sees loading state
4. AI returns structured notes in ~2 seconds
5. Tech can edit or save directly

INPUT EXAMPLE:
"Uh, replaced the capacitor, it was reading low like 15%, 
customer seemed happy, took about 45 minutes I think"

OUTPUT FORMAT:
```
REPAIRS COMPLETED:
✓ Replaced faulty run capacitor (15% below spec)

CUSTOMER INTERACTION:
✓ Customer satisfaction: Positive

TIME LOGGED:
✓ Actual time: 45 minutes | Estimated: 60 minutes
✓ Efficiency: 25% faster than estimate

JOB STATUS: Completed successfully
```

TECHNICAL IMPLEMENTATION:
- Create: app/api/ai/voice-notes/route.ts
- Create: components/tech/VoiceNoteButton.tsx
- Use OpenAI Whisper for transcription
- Use GPT-4o-mini for structuring
- Save to job_notes table (already exists)
- UI: Hold-to-record button with pulse animation

FILES TO CREATE:
- app/api/ai/voice-notes/route.ts (API endpoint)
- components/tech/VoiceNoteButton.tsx (recording UI)
- lib/ai/openai.ts (OpenAI client config)
- hooks/useVoiceRecorder.ts (audio recording hook)

---

### 2. AI PHOTO ANALYSIS - PARTS IDENTIFICATION

WHAT IT DOES:
Tech takes photo of broken part → AI identifies → Suggests replacement part
from service catalog → One-tap add to invoice

USER FLOW:
1. Tech taps "Identify Part" on job
2. Camera opens (or photo upload)
3. AI analyzes image
4. Shows: Part name, condition, replacement needed Y/N
5. Buttons: [Add to Invoice] [Not Correct?] [Manual Search]

TECHNICAL IMPLEMENTATION:
- Use OpenAI GPT-4 Vision for image analysis
- Query services table for matching parts
- Cross-reference with job's service type
- Show confidence score
- Allow manual override

FILES TO CREATE:
- app/api/ai/identify-part/route.ts
- components/tech/PartIdentifier.tsx
- components/tech/PartAnalysisResult.tsx

---

PRIORITY 1 (Build After P0 Complete)
------------------------------------

### 3. ON-SITE TROUBLESHOOTING ASSISTANT

WHAT IT DOES:
Chat interface where tech describes symptoms → AI suggests diagnostic steps

USER FLOW:
1. Tech taps "Get Help" or microphone
2. Describes: "AC blowing warm, outdoor unit not running"
3. AI responds with numbered checklist:
   - Check breaker (most common)
   - Test capacitor
   - Check contactor for 24V
4. Tech marks each step done/not done
5. If not solved, escalate to dispatch

TECHNICAL IMPLEMENTATION:
- Create chat interface (similar to ChatGPT mobile)
- Context-aware (knows job type, customer history)
- Voice input enabled
- Persist conversation to job_notes

FILES TO CREATE:
- components/tech/TroubleshootChat.tsx
- components/tech/TroubleshootMessage.tsx
- app/api/ai/troubleshoot/route.ts

---

### 4. SAFETY COMPLIANCE CHECKER ⭐ DIFFERENTIATOR

WHAT IT DOES:
AI validates photos meet safety requirements before job can be marked complete

REQUIRED PHOTOS:
BEFORE JOB:
- PPE visible (gloves, safety glasses, boots)
- Work area barricaded/tarped
- Hazards identified

AFTER JOB:
- Tools accounted for
- Site cleaned
- Hazards eliminated

USER FLOW:
1. Tech marks job "in progress" → must upload "before" photos
2. AI validates each photo
3. If missing: "Please retake - safety glasses not visible"
4. Same for "after" photos before "completed" status

TECHNICAL IMPLEMENTATION:
- Use GPT-4 Vision for validation
- Checklist stored in job metadata
- Block status changes until passed
- Soft fail: Warning vs Hard fail: Block completion

FILES TO CREATE:
- app/api/ai/safety-check/route.ts
- components/tech/SafetyChecklist.tsx
- lib/ai/safety-requirements.ts (rules config)

---

PRIORITY 2 (Nice to Have)
-------------------------

### 5. SMART JOB COMPLETION SUMMARY

WHAT IT DOES:
AI generates professional customer summary from all job data

GENERATES:
- Email-ready summary
- Plain text for SMS
- Professional notes for records

INCLUDES:
- Work performed (from voice notes + photos)
- Parts used
- Time spent
- Before/after photos
- Warranty info
- Payment link

FILES TO CREATE:
- app/api/ai/generate-summary/route.ts
- components/tech/CompletionSummary.tsx
- components/tech/ShareSummaryButton.tsx

---

### 6. SMART CUSTOMER MESSAGING

WHAT IT DOES:
AI suggests professional texts to send customer based on context

TRIGGERS:
- Running late → "Running 15 min behind..."
- Need access → "Hi, I'm at the door..."
- Job done → "All set! Here's what we did..."
- Follow-up → "How's everything working?"

FILES TO CREATE:
- components/tech/SmartMessageButton.tsx
- app/api/ai/suggest-message/route.ts
- lib/ai/message-templates.ts

---

### 7. PREDICTIVE TIME ESTIMATION

WHAT IT DOES:
AI estimates job duration based on job details, photos, and historical data

DISPLAYS ON JOB CARD:
- AI Estimate: 45-60 minutes
- Your avg on similar: 42 minutes
- Drive time: 12 minutes
- Suggested window: 2:00-3:00 PM

FILES TO CREATE:
- app/api/ai/estimate-time/route.ts
- components/tech/TimeEstimateBadge.tsx
- lib/ai/time-predictor.ts

---

UI/UX REQUIREMENTS
==================

DESIGN SYSTEM COMPLIANCE:
- Use components/ui/icons.tsx for all icons
- Use glassmorphic cards (glass-card class)
- Follow color scheme: --neon-cyan primary, --neon-emerald success
- Dark mode first, light mode support
- Mobile-optimized touch targets (min 48px)

ANIMATION REQUIREMENTS:
- AI loading: Pulsing glow effect (animate-pulse-glow-cyan)
- Recording: Red pulse animation
- Success: Quick scale-in (animate-scale-in)
- Use existing transitions from globals.css

COMMON UI PATTERNS:

AI Button Style:
```tsx
<button className="ai-action-btn">
  <Icon name="sparkles" size="sm" />  {/* or PhSparkle */}
  <span>AI Assist</span>
</button>

// CSS
.ai-action-btn {
  @apply relative overflow-hidden px-4 py-3 rounded-xl font-mono text-sm;
  background: linear-gradient(135deg, rgba(var(--neon-cyan), 0.15) 0%, rgba(var(--neon-cyan), 0.05) 100%);
  border: 1px solid rgba(var(--neon-cyan), 0.3);
  color: rgb(var(--neon-cyan));
}
```

AI Panel/Drawer:
```tsx
<div className="ai-assistant-panel glass-card-cyan">
  <div className="flex items-center gap-2 mb-4">
    <Icon name="sparkles" className="animate-pulse" />
    <span className="font-mono text-cyan-400">AI Assistant</span>
  </div>
  {/* Content */}
</div>
```

TECHNICAL REQUIREMENTS
======================

OPENAI SETUP:
- Add to .env.local: OPENAI_API_KEY
- Create lib/ai/openai.ts with client config
- Use GPT-4o-mini for cost efficiency (where appropriate)
- Use GPT-4 Vision for image analysis
- Use Whisper-1 for audio transcription

API ROUTE PATTERN:
```typescript
// app/api/ai/[feature]/route.ts
import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { openai } from '@/lib/ai/openai'

export async function POST(request: Request) {
  try {
    // 1. Auth check
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

    // 2. Get request data
    const { jobId, data } = await request.json()

    // 3. Verify tech owns this job
    const { data: job } = await supabase
      .from('jobs')
      .select('*')
      .eq('id', jobId)
      .eq('technician_id', user.id)
      .single()
    
    if (!job) return NextResponse.json({ error: 'Job not found' }, { status: 404 })

    // 4. Call AI
    const result = await callOpenAI(data)

    // 5. Return result
    return NextResponse.json({ success: true, result })
  } catch (error) {
    console.error('AI error:', error)
    return NextResponse.json({ error: 'AI processing failed' }, { status: 500 })
  }
}
```

ERROR HANDLING:
- Always fallback to manual input if AI fails
- Show "AI unavailable - type manually" option
- Log all AI errors to console (not user-facing)
- Timeout after 10 seconds

TESTING CHECKLIST:
- Test on slow 3G connection
- Test with poor audio quality
- Test with blurry photos
- Test without AI key (graceful degradation)

CODEX PROMPT (COPY THIS EXACTLY)
=================================

"Implement AI features for the technician mobile pages in Blue Collar Bot.

START WITH PRIORITY 0:
1. Voice-to-Job Notes - Create the voice recording button, API endpoint, and integration with OpenAI Whisper + GPT to transcribe and format spoken notes into professional job notes
2. AI Photo Analysis - Create part identification feature using GPT-4 Vision

THEN PRIORITY 1:
3. Troubleshooting Assistant - Chat interface for diagnostic help
4. Safety Compliance Checker - AI validation of required safety photos

FILE LOCATIONS:
- API routes: app/api/ai/[feature]/route.ts
- Components: components/tech/[ComponentName].tsx  
- Utilities: lib/ai/[utility].ts
- Hooks: hooks/use[Feature].ts

DESIGN REQUIREMENTS:
- Use existing icon system: components/ui/icons.tsx
- Follow glassmorphic design in globals.css
- Use CSS variables: rgb(var(--neon-cyan)), rgb(var(--neon-emerald))
- Mobile-first, touch-friendly (48px min targets)
- Dark mode default with light mode support

TECHNICAL STACK:
- Next.js App Router
- React + TypeScript
- OpenAI API (Whisper, GPT-4o-mini, GPT-4 Vision)
- Supabase for data
- Tailwind CSS

CRITICAL:
- All features must work on mobile browsers
- Handle errors gracefully with fallbacks
- Follow existing code patterns in the codebase
- Run `npm run lint` and `npm run build` after changes
- Create reusable components, don't duplicate code

ENVIRONMENT:
Add to .env.local: OPENAI_API_KEY=sk-...

REFERENCE:
See full specifications in: codex handoffs/codex-handoff-2026-02-13-ai-tech-features.txt"

IMPLEMENTATION ORDER
====================

PHASE 1 (Week 1):
- Voice-to-Job Notes (highest impact)
- AI Photo Analysis (differentiator)

PHASE 2 (Week 2):
- Troubleshooting Assistant
- Safety Compliance Checker

PHASE 3 (Week 3-4):
- Smart Completion Summary
- Smart Customer Messaging
- Time Estimation

SUCCESS METRICS:
- Techs save 5+ min per job on paperwork
- 80%+ use voice notes within 2 weeks
- Zero safety incidents (compliance checker)
- 20% faster average job completion

QUESTIONS?
==========
If unclear on any feature, ask for clarification before building.
Focus on QUALITY over quantity - better to have 2 perfect features than 5 broken ones.

End of handoff.
